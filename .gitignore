# ---- OS / editor ----
.DS_Store
Thumbs.db
.claude/
.vscode/

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
env/

# ---- DreamEEG dataset & derived tensors ----
# The EEG dataset is large (GBs) and openly available on figshare.
# code/dreameeg.py re-downloads any subject on demand, so we never commit it.
DreamEEG/data/

# stray large model / array outputs (keep results.csv, ignore heavy blobs)
*.pt
*.pth
DreamEEG/results/*.npy

# ---- LaTeX build artifacts ----
*.aux
*.log
*.out
*.synctex.gz
*.fls
*.fdb_latexmk
*.toc
*.lof
*.lot
*.bbl
*.blg
*.nav
*.snm

# authors verbatim run: derived data (keep the .py + .log evidence, not the arrays)
DreamEEG/code/authors_run/ani_s1_signal_sub_newset_del_ch/
*.stackdump
